spring:

  datasource:
    url: jdbc:mariadb://localhost:3306/agrify
    username: root
    password: Fida Ukwishaka
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:

    hibernate:
      ddl-auto: create

    show-sql: true

    properties:
      hibernate:
        format_sql: true

springdoc:

  swagger-ui:
    path: /swagger-ui.html